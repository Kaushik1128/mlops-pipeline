# Lean dependency set for the fraud-serving container — runtime only.
# NOT the full dev requirements.txt (no jupyter, dvc, matplotlib, etc.).
# Pinned to match what the model was trained/saved with.

bentoml==1.3.22
mlflow==2.18.0
scikit-learn==1.6.0
xgboost==3.3.0
pandas==2.2.3
numpy==2.1.3
pydantic==2.13.4
PyYAML==6.0.3
setuptools<81
# setuptools<81: mlflow's integrations still import pkg_resources, removed in
# setuptools 81. Same pin as the host requirements.txt.

[pytest]
# pythonpath = . makes the `src` package importable when running `pytest`
# from the project root (so `from src.models.evaluate import ...` works).
pythonpath = .
testpaths = tests
addopts = -q
